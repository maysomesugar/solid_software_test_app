/// {@template ColorBrightness}
/// An enum that provides color brightness variables
/// {@endtemplate}
enum ColorBrightness {
  /// A ColorBrightness field that rerurns when color brightness is light
  light,

  /// A ColorBrightness field that rerurns when color brightness is dart
  dart,
}
